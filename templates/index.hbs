<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Rust 留言板</title>
        <link href="static/css/main.css" rel="stylesheet" type="text/css" />
    </head>
    <body>
        <div class="entry">
            <h1>{{title}}</h1>
            <div class="body">
                <div class="announcement">
                    {{announcement}}
                </div>
                <nav class="nav">
                    <a href="topic_form">新增留言</a>
                </nav>
                <div class="posts">
                    {{#each posts}}
                    <div class="post">
                        <div class="title">{{topic.name}}：{{topic.title}}</div>
                        <div class="content">
                            {{topic.content}}
                            <div class="reply">
                            {{#each reply}}
                                <div class="post">
                                    <div class="title">{{name}}：{{title}}</div>
                                    <div class="content">
                                        {{content}}
                                    </div>
                                    <div class="time">留言於 {{created_time}}</div>
                                </div>
                            {{/each}}
                            </div>
                        </div>
                        <div class="time">留言於 {{created_time}}</div>
                        <div class="reply_button"><a href="reply_form/{{topic.id}}">回覆本篇留言</a></div>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </body>
</html>