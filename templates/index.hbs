<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Rust GuestBook</title>
    </head>
    <body>
        <div class="entry">
            <h1>{{title}}</h1>
            <div class="body">
                {{index_content}}
                {{#each posts}}
                <div class="post">
                    <div class="name">暱稱：{{name}}</div>
                    <div class="title">標題：{{title}}</div>
                    <div class="content">
                        {{content}}
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </body>
</html>